import * as React from 'react'

import { mainFooter } from '../styles/footer.module.css'

const FooterComponent = () => {
    return (
        <div className={ mainFooter }>
             
        </div>
    )
}

export default FooterComponent